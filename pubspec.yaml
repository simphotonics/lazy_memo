name: lazy_memo

description: Helper classes for defining lazy variables
  and memoized functions.
  Null-safety must be enabled to use this package.

version: 0.0.3-nullsafety

homepage: https://github.com/simphotonics/lazy_memo

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies: null

dev_dependencies:
  minimal_test: ^0.1.2-nullsafety
  pedantic: ^1.9.2
