name: lazy_memo

description: Helper classes for defining memoized functions and
  lazy variables that can be marked for re-initialization.
  
version: 0.1.4

homepage: https://github.com/simphotonics/lazy_memo

environment:
  sdk: '>=2.19.0 <3.0.0'

dev_dependencies:
  lints: ^2.0.1
  test: ^1.23.1
